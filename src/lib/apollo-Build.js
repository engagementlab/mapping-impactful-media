import {
    pkg
} from '@apollo/client/core/core.cjs';
const {
    ApolloClient,
    HttpLink,
    InMemoryCache,
    gql
} = pkg;
export const apollo = {
    ApolloClient,
    HttpLink,
    InMemoryCache,
    gql
}