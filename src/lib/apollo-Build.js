import pkg from '@apollo/client/core/core.cjs.js';

const {
    ApolloClient,
    HttpLink,
    InMemoryCache,
    gql
} = pkg;
export const apollo = {
    ApolloClient,
    HttpLink,
    InMemoryCache,
    gql
}